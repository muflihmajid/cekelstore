# Vue Basic Example

This example shows a simple storefront with sample products and WhatsApp checkout.

```bash
npm install
npm run dev
```

The root project also points its Vite dev entry at this example, so you can run `npm run dev` from the repository root while developing components.
